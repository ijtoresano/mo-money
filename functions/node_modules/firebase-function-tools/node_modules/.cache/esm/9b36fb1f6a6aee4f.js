let database,admin;_6a6‍.x([["default",()=>_6a6‍.o]]);_6a6‍.w("firebase-functions",[["database",["database"],function(v){database=v}]]);_6a6‍.w("firebase-admin",[["default",["admin"],function(v){admin=v}]]);


_6a6‍.d(database
  .ref('/roles/{roleUid}')
  .onWrite((eventSnap, context) => {
    // Exit when the data is not deleted.
    if (eventSnap.after.exists()) {
      return null
    }

    const roleUid = context.params.roleUid

    return admin
      .database()
      .ref(`role_grants/${roleUid}`)
      .remove()
  }));
