let functions,admin,setClaim,removeClaim;_7cb‍.x([["default",()=>_7cb‍.o]]);_7cb‍.w("firebase-functions",[["*",null,function(v){functions=v}]]);_7cb‍.w("firebase-admin",[["default",["admin"],function(v){admin=v}]]);_7cb‍.w("../../utils/customClaims",[["setClaim",["setClaim"],function(v){setClaim=v}],["removeClaim",["removeClaim"],function(v){removeClaim=v}]]);



_7cb‍.d(functions
  .region('europe-west1')
  .database.ref('admins/{uid}/')
  .onWrite(async (snap, context) => {
    const value = snap.after.val()
    const uid = context.params.uid

    if (value) {
      await admin
        .firestore()
        .doc(`/admins/${uid}/`)
        .set({ isAdmin: value })
      await customClaims.setClaim(uid, 'admin')
    } else {
      await admin
        .firestore()
        .doc(`/admins/${uid}/`)
        .delete()
      await customClaims.removeClaim(uid, 'admin')
    }

    return
  }));
