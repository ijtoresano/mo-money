let admin,createThumbnail;_660‍.x([["default",()=>_660‍anonymous]]);_660‍.w("firebase-admin",[["default",["admin"],function(v){admin=v}]]);_660‍.w("firebase-function-tools/lib/thumbnail",[["default",["createThumbnail"],function(v){createThumbnail=v}]]);


               async function _660‍anonymous(object) {
  const thumbnail = await createThumbnail(object)

  if (thumbnail) {
    const { fileDir, downloadURL } = thumbnail

    await admin
      .database()
      .ref(fileDir)
      .update({ thumbnail: downloadURL })
  }

  return
}
